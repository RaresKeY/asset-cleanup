# Current project state

## Implemented

- Project-memory shape and document maps.
- Desired pipeline, shape-aware geometry, collision, web UI, and roadmap design.
- Repository ownership, no-project-license rule, and third-party notice boundary.
- Contributor instructions and generated-data ignore rules.

## Not implemented

There is no installable Python package, processing library, CLI, web API,
frontend, container image, parser, optimizer, collision generator, or runtime
exporter in this bootstrap revision. Descriptions of those capabilities in
`design/` are desired design only.

## Compatibility

No public code, configuration, recipe, manifest, API, or artifact compatibility
contract exists yet.

## Gaps

- All runtime functionality remains to be implemented in subsequent reviewed
  slices.
